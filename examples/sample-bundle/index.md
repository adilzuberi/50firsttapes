---
okf_version: "0.1"
type: index
title: Sample bundle
---

# Sample bundle

A tiny OKF-compatible bundle for testing `kos lint`. See [hello](/notes/hello.md).

`type: index` has no registered kind, so lint warns and moves on — the OKF rule: tolerate unknown types, flag them for curation.
