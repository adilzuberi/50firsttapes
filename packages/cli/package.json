{
  "name": "@knowledge-os/cli",
  "version": "0.0.0",
  "license": "Apache-2.0",
  "type": "module",
  "bin": { "kos": "./dist/index.js" },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "node --test"
  },
  "dependencies": {
    "@knowledge-os/core": "workspace:*",
    "commander": "^12.1.0"
  }
}
