{
  "name": "@knowledge-os/mcp",
  "version": "0.0.0",
  "license": "Apache-2.0",
  "type": "module",
  "bin": { "kos-mcp": "./dist/index.js" },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "node --test"
  },
  "dependencies": {
    "@knowledge-os/core": "workspace:*"
  }
}
